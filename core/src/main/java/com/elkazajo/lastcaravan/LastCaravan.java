package com.elkazajo.lastcaravan;

import com.shatteredpixel.shatteredpixeldungeon.ShatteredPixelDungeon;
import com.watabou.utils.PlatformSupport;

/**
 * Main application entry point for LAST CARAVAN.
 *
 * The project currently reuses the Shattered Pixel Dungeon runtime
 * while LAST CARAVAN systems are introduced incrementally.
 */
public class LastCaravan extends ShatteredPixelDungeon {

    public LastCaravan(PlatformSupport platform) {
        super(platform);
    }

}